# Personal HA Add-ons — Tracktor included

- `tracktor/` — wraps ghcr.io/javedh-dev/tracktor:latest
- Auto-bump workflow (`.github/workflows/auto-bump-tracktor.yml`) keeps the add-on in sync with upstream image.
