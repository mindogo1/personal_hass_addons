name: "Tracktor (from source)"
slug: "tracktor"
version: "0.2.0"
description: "Tracktor — Vehicle tracking/fuel/maintenance manager (builds from GitHub source)."
url: "https://github.com/javedh-dev/tracktor"
arch:
  - amd64
  - aarch64
startup: "application"
init: false
stage: "stable"
ingress: false
panel_icon: "mdi:car"
webui: "http://[HOST]:[PORT:3000]/"
ports:
  "3000/tcp": 3339
ports_description:
  "3000/tcp": "Tracktor Web UI (host port editable)"
# Persisted storage is /data (provided by Supervisor). We also create symlinks so app writes there.
options:
  TZ: "Europe/Vilnius"
  AUTH_PIN: ""
  CORS_ORIGINS: ""
  PUBLIC_API_BASE_URL: ""
  GIT_REF: "dev"           # branch, tag, or commit to build
schema:
  TZ: str?
  AUTH_PIN: str?
  CORS_ORIGINS: str?
  PUBLIC_API_BASE_URL: str?
  GIT_REF: str?
