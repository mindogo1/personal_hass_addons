# TitleCardMaker (v1 CLI) — HA Add-on (Mindogo)

- Set your URLs/tokens in Configuration.
- Choose SOURCE_DIR, SERIES_YAML, CARD_DIR.
- Start the add-on; preferences are generated at `/config/preferences.yml`.
